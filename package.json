{
    "type": "module",
    "scripts": {
        "test": "node test/test.js",
        "build": "npx esbuild src/index.js --platform=browser --bundle --format=esm --outfile=dist/formula-parser.js --sourcemap=external",
        "serve-test": "sirv . -p 3000 -D"
    },
    "devDependencies": {
        "esbuild": "^0.19.8",
        "monaco-editor": "^0.45.0",
        "sirv-cli": "^2.0.2"
    }
}
